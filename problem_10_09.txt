保真模型，要保的是历史数据池
上传到保真模型的特征，一个是用于判断是否跌倒？另一个则是用于判读历史数据池是否该更新了
数据集的问题是，我们需要在连续的一个固定时间段内进行抽样，而不是一个一个时间窗口


1. 保真模型的网络结构示意图
2. 不更新时的三种处理方式：全零，follow，插值
3. 用于保真模型训练的数据集的构建（多样化、多比例、随机性） --> 训练数据的构建也是对采样的模拟，由于构建过程是随机的，必然会（因为采样）形成 easy samples，以及 hard samples，这一差异性的信号会在后续用于训练采样模型
4. 当前的数据摘要本身就是为了跌倒检测适配的，所以保真模型精度很高是合理的，后续替换掉特征模型的任务后会有改变